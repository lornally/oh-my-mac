###### 搞定github

* 参考readme

###### fuck macos

1. 设置-》桌面与程序坞-〉拖移窗口至菜单栏以充满屏幕. 纯傻逼功能.
2. 备忘录-》设置-〉始终回到上个快速备忘录. 纯脑残功能.
3. finder-》 设置-〉高级-》 只有第一项打勾(显示所有文件扩展名). 傻逼的默认选项.
4. Terminal ->设置->新窗口同目录打开. 傻逼的默认设置.

###### 手动安装

* typora
* 丁丁
* chrome
* 搜狗拼音

###### chrome

```sh
chrome://flags/#enable-force-dark
```

###### zsh

```sh
sh -c "$(curl -fsSL https://gitee.com/pocmon/ohmyzsh/raw/master/tools/install.sh)"
# config
open ~/.zshrc

# 修改内容
plugins=(git z)
# 代理设置(以clashx举例)
alias neton="export all_proxy=socks5://127.0.0.1:7890"
alias httpon="export all_proxy=http://127.0.0.1:7890"
alias netoff="unset all_proxy"
```

###### brew

```sh
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"


brew install   omnidisksweeper  textmate alt-tab sublime-text  anaconda   vlc notion git-lfs zed visual-studio-code the-unarchiver qbittorrent cursor freeplane karabiner-elements
```

###### git config

```sh
open  ~/.gitconfig
```

文件

```sh
# gitconfig文件
[user]
  name = bergman
  email = 2025.1103sequoia@macstudio2022
[core]
  quotepath =false
  editor = mate --wait
  #! 这里千万记得要改为真实地址, 全局ignore配置mac的DS_store文件: echo ".DS_Store" >> .gitignore
  excludesfile = /Users/machangkun/x/.gitignore_g
[pull]
  rebase = true
# 处理大文件, 可以放在每个项目的目录下的.git/config
[alias]
  track-large = "!git st --porcelain --ignore-submodules | grep -v 'D ' | cut -b4- | sed -n 's/\\(\\(.* -> \\)\\|\\)\\(.\\)/\\3/p' | xargs -I{} find {} -size +300k | xargs -I{} git lfs track {}"
```

lfs

```sh
brew install git-lfs # 为了顺畅处理大文件, 需要安装这个.
git lfs install  # 这个update global git config
```

###### vscode cursor

* python 微软
* pylance 微软
* pylint 微软 据说可以不装....但是我装了
* ruff 发布者: astral, 标识符: charliermarsh.ruff
* roo
* 灵码
* markdown all in one

```sh
brew install python@3.12
python3.12 -m venv /Users/x/.pysidenv
source /Users/x/.pysidenv/bin/activate
# 然后平平无奇的安装包, 最后一个是ipynb
pip install pyside6 Nuitka ipykernel

# 把python环境注册为ipynb的环境中的一个内核, 这里面两个pysidenv都可以随意, 只要能认出来就行.
python -m ipykernel install --user --name pysidenv --display-name "pysidenv"
```

1. 便捷选择: vscode的右下角会要求你选择解释器.
2. 手动选择: cmd + shift + p  -> python: select interpreter(解释器)