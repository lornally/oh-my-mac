2. maxfiles 256 会导致 VS Code 缓慢吗？
会的！ 你的项目有 38151 个文件，而系统限制是 256，远远不够。

这会导致：

VS Code 无法监视所有文件变化
文件保存后状态不更新
Git 状态不实时刷新


我的疑问是, 这样会导致系统运行缓慢吗? 项目就算有3万个文件, 我又不会打开3万个文件.

* 20:30
那么这个设置是系统级的吗? 他和vscode是啥关系? 都不在setting里面的.
# 立即生效
ulimit -n 65536

# 永久生效（写入 zshrc）
echo 'ulimit -n 65536' >> ~/.zshrc

* 20:33
kimi在这件事上基本是胡说八道, 他说teriminal 的code .方式启动会使用ulimit, 