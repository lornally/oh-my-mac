# 技术目录全面分析计划

## 目标
全面分析 `/Users/bergman/X/oh-my-project/技术` 目录，了解各项目技术栈，生成合理的 `.vscode/settings.json` 配置。

## 当前已知信息

### 主要子目录
| 目录 | 初步观察 | 预估技术栈 |
|------|---------|-----------|
| `脑图_keydog_贱狗_已经独立` | PySide 项目，有 build/__pycache__/log | Python + PySide6 |
| `浏览器插件_luo` | 浏览器插件 | JavaScript/TypeScript |
| `测试工具_bud` | 测试工具 | 待分析 |
| `打包_bag` | 打包工具 | 待分析 |
| `日常工具_mck/python工具` | Python 工具集 | Python |
| `语言和他的编辑器和编译器teapug` | 语言相关 | 待分析 |

### 已知技术栈（用户提供）
- Python + PySide
- JavaScript/CoffeeScript
- Ruby
- C + Qt
- Rust

---

## 分析阶段

### 阶段一：目录结构映射

**任务：**
1. 统计每个子目录的文件数量
2. 识别所有依赖管理目录（node_modules, venv, .venv, target 等）
3. 识别所有构建输出目录（dist, build, out, target 等）
4. 识别所有缓存目录（__pycache__, .pytest_cache, .mypy_cache, .ruff_cache 等）

**输出：**
- 目录结构树状图
- 文件数量统计表

---

### 阶段二：逐个分析主要项目

#### 2.1 脑图_keydog_贱狗_已经独立
**检查内容：**
- [ ] Python 版本（pyproject.toml, requirements.txt, setup.py）
- [ ] 依赖管理方式（pip, poetry, conda, uv）
- [ ] 虚拟环境位置（.venv/, venv/）
- [ ] 构建配置（PyInstaller, cx_Freeze 等）
- [ ] 生成的文件（.app, .exe, .dmg）
- [ ] 日志文件（log/ 目录内容）
- [ ] 是否可以排除：build/, __pycache__/, log/ 的某些子目录

#### 2.2 浏览器插件_luo
**检查内容：**
- [ ] 是否有 package.json
- [ ] 是否有 node_modules/
- [ ] 构建工具（webpack, vite, rollup）
- [ ] 输出目录（dist/, build/）
- [ ] 源码目录结构

#### 2.3 测试工具_bud
**检查内容：**
- [ ] 技术栈
- [ ] 依赖目录
- [ ] 构建输出

#### 2.4 打包_bag
**检查内容：**
- [ ] 技术栈
- [ ] 依赖目录
- [ ] 构建输出

#### 2.5 日常工具_mck/python工具
**检查内容：**
- [ ] Python 版本
- [ ] 依赖管理
- [ ] 是否有虚拟环境

#### 2.6 语言和他的编辑器和编译器teapug
**检查内容：**
- [ ] 技术栈
- [ ] 是否有编译输出
- [ ] 依赖目录

---

### 阶段三：生成遍历文档

**文档名：** `技术目录技术栈清单.md`

**内容结构：**
```markdown
# 技术目录技术栈清单

## 1. 脑图_keydog_贱狗_已经独立
- **技术栈**: Python + PySide6
- **依赖管理**: Poetry/pip/...
- **虚拟环境**: .venv/ (大小: xxx MB)
- **构建输出**: build/ (大小: xxx MB)
- **排除建议**:
  - `**/脑图_keydog_贱狗_已经独立/build/**` → watcherExclude
  - `**/脑图_keydog_贱狗_已经独立/__pycache__/**` → watcherExclude + exclude
  - `**/脑图_keydog_贱狗_已经独立/log/**` → watcherExclude（视情况而定）

## 2. 浏览器插件_luo
...
```

---

### 阶段四：生成最终配置

**基于分析结果生成：** 设置建议.md

```json

{
  "files.exclude": {
    // 根据分析结果生成的配置
  },
  "files.watcherExclude": {
    // 根据分析结果生成的配置
  },
  "search.exclude": {
    // 根据分析结果生成的配置
  }
}
```

---

## 执行计划

| 阶段 | 预计时间 | 依赖 |
|------|---------|------|
| 阶段一 | 10 分钟 | 无 |
| 阶段二 | 每个项目 10-15 分钟 | 阶段一 |
| 阶段三 | 20 分钟 | 阶段二 |
| 阶段四 | 10 分钟 | 阶段三 |

---

## 不必用户反馈, kimi直接顺序执行, 但是, 每个阶段都要输出一份报告

请在每个阶段完成后提供报告文档：

1. **阶段完成后** 统计结果
2. **每个项目分析完成后**：
   - 技术栈识别
   - 列表: 哪些文件/目录是**必须保留**的（源代码、配置文件等）
   - 列表: 哪些文件/目录是**可以排除**的（生成的、临时的）
3. 本阶段的 建议配置文件

---

## 开始执行

顺序执行, 直到都完成, 生成 总设置建议.md 为止
