###### 安装
```sh
# 正常安装会失败:  brew install php@8.2

brew untap shivammathur/php 2>/dev/null; brew tap shivammathur/php
brew install shivammathur/php/php@8.2

```

To enable PHP in Apache add the following to httpd.conf and restart Apache:
    LoadModule php_module /usr/local/opt/php@8.2/lib/httpd/modules/libphp.so

    <FilesMatch \.php$>
        SetHandler application/x-httpd-php
    </FilesMatch>

Finally, check DirectoryIndex includes index.php
    DirectoryIndex index.php index.html

The php.ini and php-fpm.ini file can be found in:
    /usr/local/etc/php/8.2/

php@8.2 is keg-only, which means it was not symlinked into /usr/local,
because this is an alternate version of another formula.

If you need to have php@8.2 first in your PATH, run:
  echo 'export PATH="/usr/local/opt/php@8.2/bin:$PATH"' >> ~/.zshrc
  echo 'export PATH="/usr/local/opt/php@8.2/sbin:$PATH"' >> ~/.zshrc

For compilers to find php@8.2 you may need to set:
  export LDFLAGS="-L/usr/local/opt/php@8.2/lib"
  export CPPFLAGS="-I/usr/local/opt/php@8.2/include"

To start shivammathur/php/php@8.2 now and restart at login:
  brew services start shivammathur/php/php@8.2
Or, if you don't want/need a background service you can just run:
  /usr/local/opt/php@8.2/sbin/php-fpm --nodaemonize



###### 有个警告
==> Patching
==> Applying gcc-15.1.0.diff
==> ../configure --prefix=/usr/local/opt/gcc --libdir=/usr/local/opt/gcc/lib/gcc
==> gmake BOOT_LDFLAGS=-Wl,-headerpad_max_install_names
==> gmake install DESTDIR=/private/tmp/gcc-20251117-51327-skcjrg/gcc-15.2.0/buil
Warning: The post-install step did not complete successfully
You can try again using:
  brew postinstall gcc
==> Summary
🍺  /usr/local/Cellar/gcc/15.2.0: 2,085 files, 585.7MB, built in 193 minutes 12 seconds
==> Installing shivammathur/php/php@8.2
==> Patching
==> ./buildconf --force
==> ./configure --localstatedir=/usr/local/var --sysconfdir=/usr/local/etc/php/8.2
==> make
==> make install
==> /usr/local/Cellar/php@8.2/8.2.29_2/bin/phpize

解决办法:
```sh
# 重试gcc的后续配置
brew postinstall gcc

# 如果还不行，强制重新安装
brew reinstall gcc
```


###### php OK
```sh
# 这个会报错
brew install php@8.2 composer

# 这个可以
brew link --overwrite php@8.2

php -v
```

###### 数据库
```sh
# 安装MariaDB（比MySQL轻量，兼容性好）
brew install mariadb
brew services start mariadb

# 安全初始化（设置root密码等）
mysql_secure_installation
```


###### 服务器
```sh
brew install nginx  # 或 apache
brew services start nginx
# 测试访问 http://localhost:8080

```

###### 恢复
```sh
# 1. 解压备份到Web目录（例如/usr/local/var/www/wiki）
sudo cp -r data/* /usr/local/var/www/wiki/

# 2. 恢复数据库（假设是.sql文件）
mysql -uroot -p < database.sql

# 3. 修改LocalSettings.php中的数据库密码
# 4. 运行更新脚本
cd /usr/local/var/www/wiki
php maintenance/update.php
```


###### 扩展
```sh
# 看PHP模块
php -m | grep apcu
# 必需扩展（macOS用Homebrew安装PHP时大部分已包含）
#php -m | grep -E "(gd|intl|mbstring|xml|curl|openssl|pdo_mysql|apcu)"

# 如有缺失，这样安装（例如apcu）
pecl install apcu
# 然后编辑php.ini添加extension=apcu.so
```

